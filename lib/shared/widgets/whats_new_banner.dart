// The "What's new in v2.5" banner advertised Scalp mode (0DTE), which was
// removed from the app in July 2026 — the app is swing+leaps focused.
// This widget has zero remaining callers (removed from home_screen.dart).
//
// Left as an empty file (rather than deleted) because the sandbox this
// change was made from couldn't delete files on the mounted project
// folder. Safe to delete this file manually — nothing imports it.

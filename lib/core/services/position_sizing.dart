// Options position sizing (account size / max risk % -> contract qty) was
// removed from the app in July 2026 along with the rest of the options
// subsystem — the bot is stock-only now. This service has no remaining
// callers.
//
// Left as a near-empty file (rather than deleted) because the sandbox this
// change was made from couldn't delete files on the mounted project folder.
// Safe to delete this file manually.
